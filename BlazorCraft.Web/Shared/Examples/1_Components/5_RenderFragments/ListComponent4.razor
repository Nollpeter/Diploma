@typeparam T 
@* ^^ Notice the typeparam*@ 

@Title
@foreach (var item in ListItems)
{
    @ListItemTemplate(item)
}
@code{

    [Parameter, EditorRequired] public RenderFragment Title { get; set; }
    [Parameter, EditorRequired] public RenderFragment<T> ListItemTemplate { get; set; } // Notice the new generic parameter
    [Parameter, EditorRequired] public List<T> ListItems { get; set; } // Notice the new generic parameter

}